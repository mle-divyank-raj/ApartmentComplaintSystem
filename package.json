{
  "name": "acls-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/ResidentApp.Web",
    "build": "npm run build --workspace=apps/ResidentApp.Web",
    "build:packages": "echo \"Packages use TypeScript source directly — no build step needed. Run 'npm run build' to build the Next.js app.\""
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
